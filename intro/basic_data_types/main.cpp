#include <bits/stdc++.h>
#include <limits>

using namespace std;

void tc() {
    long long n, k;
    string s;
    double pi;
    cin >> n >> k >> s >> pi;
    cout.precision(2);
    cout << n << " " << fixed << k << " " << s << " " << pi << "\n";
}

int main() {
    int t;
    cin >> t;
    while (t--) tc();
}
